<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="Inicio"
        android:icon="@drawable/ic_home"
        android:id="@+id/inicioAdmin"
        android:textColor="#FFFFFF"
        android:iconTint="#FFFFFF"
        />
    <item android:title="Mensajes"
        android:icon="@drawable/ic_message"
        android:id="@+id/mensajesAdmin"
        android:textColor="#FFFFFF"
        android:iconTint="#FFFFFF"
        />
    <item android:title="Usuarios"
        android:icon="@drawable/ic_user"
        android:id="@+id/usuariosAdmin"
        android:textColor="#FFFFFF"
        android:iconTint="#FFFFFF"
        />
    <item android:title="Sitios"
        android:icon="@drawable/ic_user"
        android:id="@+id/sitiosAdmin"
        android:textColor="#FFFFFF"
        android:iconTint="#FFFFFF"
        />
    <item android:title="Equipos"
        android:icon="@drawable/ic_user"
        android:id="@+id/equiposAdmin"
        android:textColor="#FFFFFF"
        android:iconTint="#FFFFFF"
        />
</menu>